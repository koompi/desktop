[package]
name = "koompi-power"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Battery, charge threshold and power profiles over UPower and power-profiles-daemon"

[dependencies]
koompi-service.workspace = true
tokio.workspace = true
zbus.workspace = true
zvariant.workspace = true
futures-util.workspace = true

[dev-dependencies]
# a socket pair standing in for UPower, so the backpressure test needs no bus
koompi-service = { workspace = true, features = ["test-support"] }
