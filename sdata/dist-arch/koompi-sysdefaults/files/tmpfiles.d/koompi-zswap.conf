# Arch's kernel enables zswap by default. In front of swap on zram it only
# compresses every page twice and breaks zramctl's accounting. Boot-only (w!),
# so switching it on by hand for an experiment lasts until reboot.
w! /sys/module/zswap/parameters/enabled - - - - N
