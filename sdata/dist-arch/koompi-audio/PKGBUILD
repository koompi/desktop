groups=(koompi)
pkgname=koompi-audio
pkgver=1.0
pkgrel=5
pkgdesc='KOOMPI Audio Dependencies'
arch=(any)
license=(None)
depends=(
  cava
  pavucontrol-qt
  wireplumber
  pipewire-pulse
  sound-theme-freedesktop
  libdbusmenu-gtk3
  playerctl
  # The sidebar ships an EasyEffects toggle and execs.lua starts it in service
  # mode at login. Both no-op when it is absent, which is a toggle that does
  # nothing on a desktop that means to have equalization.
  easyeffects
)
